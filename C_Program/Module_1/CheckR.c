#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b;
    printf("Enter no. a: ");
    scanf("%d",&a);
    printf("Enter no. b: ");
    scanf("%d",&b);
    if(a==b)
    {
    printf("Given two numbers are equal");}else{printf("Given two numbers are not equal");}
    return 0;
}
